# AGENTS.md

이 문서는 이 저장소에서 작업하는 에이전트와 기여자가 따라야 할 기본 개발 지침입니다.

## 기본 원칙

- 코드는 Python으로 작성합니다.
- 백엔드 API는 FastAPI를 우선 활용합니다.
- 프론트엔드 서버 상태 관리는 TanStack Query를 우선 활용합니다.
- 설계와 구현은 SOLID 설계 원칙을 준수합니다.
- 변경 범위는 요청된 작업과 직접 관련된 부분으로 제한합니다.
- 기존 코드 스타일, 디렉터리 구조, 네이밍 규칙이 생기면 이를 우선 따릅니다.

## SOLID 설계 지침

- 단일 책임 원칙: 클래스, 함수, 모듈은 하나의 명확한 책임을 갖도록 작성합니다.
- 개방-폐쇄 원칙: 기존 동작을 불필요하게 수정하기보다 확장 가능한 구조를 우선 고려합니다.
- 리스코프 치환 원칙: 하위 타입은 상위 타입의 계약을 깨지 않아야 합니다.
- 인터페이스 분리 원칙: 사용하지 않는 기능에 의존하지 않도록 작고 명확한 인터페이스를 선호합니다.
- 의존성 역전 원칙: 구체 구현보다 추상화와 명확한 경계에 의존하도록 설계합니다.

## Python 및 FastAPI 지침

- FastAPI 라우터, 서비스, 스키마, 설정, 외부 연동 코드를 역할별로 분리합니다.
- 비즈니스 로직은 가능하면 라우터 내부에 직접 두지 않고 서비스 계층으로 분리합니다.
- 요청/응답 모델은 Pydantic 모델로 명확히 정의합니다.
- 예외 처리는 사용자에게 필요한 정보와 내부 디버깅 정보를 구분합니다.
- I/O, 네트워크, 데이터베이스 호출은 테스트하기 쉬운 경계로 감쌉니다.
- 타입 힌트를 적극적으로 사용하고, 모호한 `Any` 사용은 피합니다.

## TanStack Query 지침

- 서버 상태 조회와 변경은 TanStack Query의 query/mutation 패턴을 사용합니다.
- query key는 재사용 가능하고 충돌이 없도록 일관된 구조로 정의합니다.
- API 호출 함수와 UI 컴포넌트 책임을 분리합니다.
- 낙관적 업데이트, 캐시 무효화, 재시도 정책은 사용자 경험과 데이터 정합성을 함께 고려해 적용합니다.
- 로딩, 에러, 빈 상태를 명확하게 처리합니다.

## PR 작성 지침

- PR 제목과 본문은 한글로 작성합니다.
- PR 본문에는 변경 목적, 주요 변경 사항, 테스트 결과를 포함합니다.
- 변경 사항이 사용자 동작이나 API 계약에 영향을 주는 경우 이를 명확히 설명합니다.
- 확인하지 않은 내용을 사실처럼 쓰지 않습니다.

## PR 리뷰 대응 지침

- PR 리뷰 댓글을 처리할 때는 각 댓글이 실제 코드와 요구사항에 근거한 수정 요청인지, 잘못된 전제나 환각에 기반한 의견인지 판단합니다.
- 수정이 필요한 댓글이면 코드를 반영한 뒤 해당 리뷰 댓글에 한글로 답변하고 `수정 완료` 또는 `수정 예정` 상태를 명시합니다.
- 환각으로 판단되는 댓글이면 근거를 확인한 뒤 해당 리뷰 댓글에 한글로 답변하고 `환각으로 판단` 상태와 판단 근거를 명시합니다.
- 일부만 타당한 댓글이면 반영한 부분과 반영하지 않은 부분을 구분해 `부분 반영` 상태로 답변합니다.
- 판단이 어려운 댓글은 임의로 처리하지 않고 `확인 필요` 상태로 질문을 남깁니다.

## 검증 지침

- 변경 후 관련 테스트, 타입 검사, 린트가 존재하면 실행합니다.
- 테스트를 실행하지 못했거나 테스트가 없는 경우 PR 본문이나 작업 요약에 그 이유를 명시합니다.
- 버그 수정 시에는 가능하면 회귀 테스트를 추가합니다.
